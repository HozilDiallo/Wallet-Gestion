# Copiez vers .env et adaptez vos identifiants locaux (optionnel).
# Les variables DB_* sont lues par config/database.php si présentes dans l'environnement.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=wallet_gestion
DB_USER=root
DB_PASSWORD=
